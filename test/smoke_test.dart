import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App version sanity', () {
    // Smoke test: ensures the test harness wires up correctly in CI.
    expect(1 + 1, equals(2));
  });
}
